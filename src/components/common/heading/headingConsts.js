import { variantProps } from "../../styled";

const headingProps = {
    variant: variantProps,
    headingStyle: {
        circle: 'circle',
        square: 'square'
    }
};

export {
    headingProps
};