import { variantProps } from "../../styled";

const bodyOverflowProps = {
    hidden: 'hidden',
    initial: 'initial'
};

const modalProps = {
    variant: variantProps
};

export {
    bodyOverflowProps,
    modalProps
};