const intialWindowWidth = window.innerWidth;

const useMediaQueryEvent = "resize";

const minDesktopWidth = 992;

const minTabletWidth = 600;

const minMobileWidth = 320;

export {
    intialWindowWidth,
    useMediaQueryEvent,
    minDesktopWidth,
    minTabletWidth,
    minMobileWidth
};